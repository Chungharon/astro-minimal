// @ts-ignore: No type declarations found for 'astro-i18next'
import type { AstroI18nextConfig } from "astro-i18next";

const config: AstroI18nextConfig = {
  defaultLocale: "en",
  locales: ["en", "it"],
};

export default config;